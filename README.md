# Mapping Access to Social Programs

An interactive demonstration of mapping administrative procedures and exploring applicant–worker encounters.

This is an illustrative research mockup. The scenarios, tool activity and outcome counts are authored examples, not empirical findings or live agent runs.

The standalone `index.html` requires no build or external dependencies. GitHub Pages serves the repository root on `main`.
